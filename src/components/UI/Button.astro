---
export interface Props {
  href?: string;
  variant?: "primary" | "secondary" | "accent" | "outline";
  size?: "sm" | "md" | "lg";
  class?: string;
  target?: string;
  rel?: string;
  disabled?: boolean;
  type?: "button" | "submit" | "reset";
  ariaLabel?: string;
}

const {
  href,
  variant = "primary",
  size = "md",
  class: className = "",
  target,
  rel,
  disabled = false,
  type = "button",
  ariaLabel,
  ...rest
} = Astro.props;

const baseStyles = [
  "inline-flex items-center justify-center gap-2",
  "font-sans text-base font-medium",
  "border-2 rounded-lg",
  "transition-all duration-300 ease-in-out",
  "focus:outline-none focus:ring-2 focus:ring-offset-2",
  "disabled:opacity-50 disabled:cursor-not-allowed",
  "min-h-[44px] min-w-[44px]", // Mobile touch target
  "transform hover:scale-105 active:scale-95",
];

const sizeStyles = {
  sm: "px-4 py-2 text-sm",
  md: "px-6 py-3",
  lg: "px-8 py-4 text-lg",
};

const variantStyles = {
  primary: [
    "bg-gradient-to-r from-primary-600 to-primary-700 text-white border-primary-600",
    "hover:from-primary-700 hover:to-primary-800 hover:border-primary-700",
    "active:from-primary-800 active:to-primary-900",
    "focus:ring-primary-500",
    "shadow-md hover:shadow-lg hover:shadow-primary-700/25",
  ].join(" "),
  secondary: [
    "bg-gradient-to-r from-primary-700 to-primary-800 text-white border-primary-700",
    "hover:from-primary-800 hover:to-primary-900 hover:border-primary-800",
    "active:from-primary-900 active:to-primary-950",
    "focus:ring-primary-500",
    "shadow-md hover:shadow-lg hover:shadow-primary-800/25",
  ].join(" "),
  accent: [
    "bg-gradient-to-r from-amber-600 to-amber-700 text-white border-amber-500/30",
    "hover:from-amber-500 hover:to-amber-600 hover:border-amber-400/50",
    "active:from-amber-700 active:to-amber-800",
    "focus:ring-amber-400",
    "shadow-md hover:shadow-lg hover:shadow-amber-600/25",
  ].join(" "),
  outline: [
    "bg-white text-gray-900 border-gray-300",
    "hover:bg-gray-50 hover:text-black hover:border-gray-400",
    "active:bg-gray-100",
    "focus:ring-primary-500",
    "shadow-sm hover:shadow-md",
  ].join(" "),
};

const classes = [
  ...baseStyles,
  sizeStyles[size],
  variantStyles[variant],
  className,
].join(" ");

const Element = href ? "a" : "button";
---

<Element
  href={href}
  class={classes}
  target={target}
  rel={target === "_blank" ? "noopener noreferrer" : rel}
  disabled={disabled}
  type={!href ? type : undefined}
  aria-label={ariaLabel}
  role={!href ? "button" : undefined}
  {...rest}
>
  <slot />
</Element>

<style>
  /* Ensure proper touch target size on mobile */
  @media (max-width: 640px) {
    a,
    button {
      min-height: 44px;
      min-width: 44px;
    }
  }
</style>
