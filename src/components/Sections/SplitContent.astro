---
import ResponsivePicture from "../UI/ResponsivePicture.astro";

const { header, copy, columns = [], background_color = "" } = Astro.props;

const normalizedColumns = columns.slice(0, 2).map((column) => ({
  ...column,
  items:
    column?.items?.map((item) => ({
      ...item,
      copySegments: Array.isArray(item?.copy)
        ? item.copy
        : item?.copy
          ? [item.copy]
          : [],
    })) ?? [],
}));
---

<section
  class="px-6 py-12 md:py-20 lg:py-24"
  style={{ backgroundColor: background_color ?? "" }}
>
  <div class="container">
    {
      (header || copy) && (
        <div class="text-center mb-16">
          {header && <h2 class="text-3xl md:text-5xl font-bold">{header}</h2>}
          {copy && (
            <p class="text-3xl text-gray-700 max-w-3xl mx-auto">{copy}</p>
          )}
        </div>
      )
    }
  </div>
  <div
    class:list={[
      "grid w-full max-w-5xl mx-auto gap-10 md:gap-14",
      normalizedColumns.length > 1
        ? "grid-cols-1 md:grid-cols-2"
        : "grid-cols-1 max-w-3xl",
    ]}
  >
    {
      normalizedColumns.map((column) => (
        <div class="flex flex-col gap-10 md:gap-14">
          {column.items?.map((item) =>
            item?.image ? (
              <figure class="flex flex-col gap-4">
                <ResponsivePicture
                  src={item.image}
                  alt={item.image_alt ?? ""}
                  class="overflow-hidden rounded-[32px] shadow-md"
                  imageClass="h-full w-full object-cover"
                />
                {item?.legend && (
                  <figcaption class="text-sm leading-relaxed text-primary-800 md:text-base">
                    {item.legend}
                  </figcaption>
                )}
              </figure>
            ) : (
              <article
                class:list={[
                  "flex flex-col gap-4 text-primary-800",
                  item?.boxed
                    ? "rounded-[32px] bg-white p-8 shadow-lg md:p-10"
                    : "",
                  normalizedColumns.length === 1 && "text-center",
                ]}
              >
                {item?.heading && (
                  <h3 class="text-2xl font-bold tracking-wide text-primary-900 md:text-4xl">
                    {item.heading}
                  </h3>
                )}
                {item?.copySegments?.map((paragraph) => (
                  <p class="text-lg leading-relaxed md:text-xl">{paragraph}</p>
                ))}
                {item?.meta && <p class="text-base md:text-lg">{item.meta}</p>}
              </article>
            )
          )}
        </div>
      ))
    }
  </div>
</section>
