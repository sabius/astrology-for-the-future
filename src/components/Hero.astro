---
const { header, copy, button } = Astro.props;
---
<section class="hero">
  <h1 class="text-6xl font-medium">{header}</h1>
  <p class="text-3xl font-medium">{copy}</p>
  <!-- {button && <a href={button.url} class="button">{button.text}</a>} -->
</section>

<style>
  .hero { text-align: center; padding: 4rem 1rem; background-color: #f0f4f8; }
</style>
